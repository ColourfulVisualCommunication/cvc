# Copy to .env. In development the Vite proxy handles /api, so this can stay empty.
# In production set it to the deployed Flask URL, e.g.
# VITE_API_URL=https://cvc-api.onrender.com/api/v1
VITE_API_URL=
